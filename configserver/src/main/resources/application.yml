server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
      microservice:
        example:
          greetings: "Hello from Config Server!"
